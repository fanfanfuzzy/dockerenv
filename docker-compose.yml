version: '3.8'

services:
  group-equivariant-net:
    build: .
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    volumes:
      - ./data:/workspace/data
    ports:
      - "8888:8888"
    command: jupyter notebook --ip 0.0.0.0 --port 8888 --no-browser --allow-root
